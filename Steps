setup the simple express server

then create the docker file 

FROM node:22-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3000

CMD ["node", "index"]

then create the build the docker image
docker build -t api .

then run the docker image
docker run -p 3000:3000 api  or docker run -it --rm -p 3000:3000 api



now create the docker compose file





